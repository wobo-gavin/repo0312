<?xml version="1.0" encoding="UTF-8"?>

<project name="/* Replaced /* Replaced /* Replaced guzzle */ */ */" default="init">

    <!-- ============================================  -->
    <!-- Target: init-git                              -->
    <!-- ============================================  -->
    <target name="init-git">
        <echo msg="Initializing git submodules" />
        <exec command="git submodule init" dir="./.." />
        <exec command="git submodule update" dir="./.." />
    </target>

    <!-- ============================================  -->
    <!-- Target: init-test                             -->
    <!-- ============================================  -->
    <target name="init-test">
        <echo msg="Copying ./phpunit.xml.dist to ./phpunit.xml" />
        <copy file="../phpunit.xml.dist" tofile="../phpunit.xml" />
    </target>

    <!-- ============================================  -->
    <!-- (DEFAULT)  Target: init                       -->
    <!-- ============================================  -->
    <target name="init" depends="init-git,init-test" />

    <!-- ============================================  -->
    <!-- Target: phar                                  -->
    <!-- ============================================  -->
    <target name="phar">
        <pharpackage
            destfile=".//* Replaced /* Replaced /* Replaced guzzle */ */ */.phar"
            basedir="../src"
            stub="./autoload.php"
            alias="/* Replaced /* Replaced /* Replaced Guzzle */ */ */"
            signature="sha1">
            <fileset dir="../src">
                <include name="**/*.php" />
                <exclude name="**/Tests/**" />
            </fileset>
            <metadata>
                <element name="version" value="1.0" />
            </metadata>
        </pharpackage>
    </target>

    <!-- ============================================  -->
    <!-- Target: template                              -->
    <!-- ============================================  -->
    <target name="template">
        <input propertyname="service.path" promptChar=":">Enter the path where you want to install your web service /* Replaced /* Replaced /* Replaced client */ */ */</input>
        <input propertyname="service.name" promptChar=":">Enter the name of your /* Replaced /* Replaced /* Replaced client */ */ */ project (e.g. /* Replaced /* Replaced /* Replaced Guzzle */ */ */ AWS)</input>
        <input propertyname="service./* Replaced /* Replaced /* Replaced client */ */ */_class" promptChar=":">Enter the name of your /* Replaced /* Replaced /* Replaced client */ */ */ class (e.g. UnfuddleClient)</input>
        <input propertyname="service.xml" promptChar=":" defaultValue="n" validArgs="y,n">Will your /* Replaced /* Replaced /* Replaced client */ */ */ use an XML file for dynamic commands?</input>
        <input propertyname="service.short_name" promptChar=":">Enter the short name of your /* Replaced /* Replaced /* Replaced client */ */ */ (e.g. /* Replaced /* Replaced /* Replaced guzzle */ */ */-aws)</input>
        <input propertyname="service.namespace" promptChar=":">Enter the root namespace of your /* Replaced /* Replaced /* Replaced client */ */ */ (e.g. /* Replaced /* Replaced /* Replaced Guzzle */ */ */\Aws)</input>
        <input propertyname="service.relative_path" promptChar=":">Enter the relative path of where your /* Replaced /* Replaced /* Replaced client */ */ */ should be installed relative to the /* Replaced /* Replaced /* Replaced Guzzle */ */ */\ namespace folder (e.g. /* Replaced /* Replaced /* Replaced Guzzle */ */ *//Aws)</input>
        <input propertyname="service.author" promptChar=":">Enter your full name</input>
        <input propertyname="service.email" promptChar=":">Enter your email address</input>
        <input propertyname="service.git_url" promptChar=":" defaultValue="">Enter the URL to the project git repository</input>

        <echo msg="Creating web service /* Replaced /* Replaced /* Replaced client */ */ */ project" />

        <fileset id="no-xml" dir="./template">
            <include name="**/**"/>
            <exclude name="Client-Xml.php"/>
        </fileset>
        <fileset id="xml" dir="./template">
            <include name="**/**"/>
            <exclude name="Client.php"/>
        </fileset>
        <if>
            <equals arg1="${service.xml}" arg2="y" />
            <then>
                <property name="service.file_list" value="xml" />
            </then>
            <else>
                <property name="service.file_list" value="no-xml" />
            </else>
        </if>

        <echo msg="Creating folder for project at ${service.path}" />
        <mkdir dir="${service.path}" />

        <echo msg="Copying files to folder" />
        <copy todir="${service.path}" overwrite="true">
            <fileset refid="${service.file_list}" />
            <filterchain>
                <expandproperties />
            </filterchain>
        </copy>
    </target>
</project>