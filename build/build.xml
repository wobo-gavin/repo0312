<?xml version="1.0" encoding="UTF-8"?>

<project name="/* Replaced /* Replaced /* Replaced guzzle */ */ */" default="init">

    <!-- ============================================  -->
    <!-- Target: init-git                              -->
    <!-- ============================================  -->
    <target name="init-git">
        <echo msg="Initializing git submodules" />
        <exec command="git submodule update --init" dir="./.." />
    </target>

    <!-- ============================================  -->
    <!-- Target: init-test                             -->
    <!-- ============================================  -->
    <target name="init-test">
        <echo msg="Copying ./phpunit.xml.dist to ./phpunit.xml" />
        <copy file="../phpunit.xml.dist" tofile="../phpunit.xml" />
    </target>

    <!-- ============================================  -->
    <!-- (DEFAULT)  Target: init                       -->
    <!-- ============================================  -->
    <target name="init" depends="init-git,init-test" />

    <!-- ============================================  -->
    <!-- Target: phar                                  -->
    <!-- ============================================  -->
    <target name="phar">
        <pharpackage
            destfile=".//* Replaced /* Replaced /* Replaced guzzle */ */ */.phar"
            basedir="../src"
            stub="./autoload.php"
            alias="/* Replaced /* Replaced /* Replaced Guzzle */ */ */"
            signature="sha1">
            <fileset dir="../src">
                <include name="**/*.php" />
                <exclude name="**/Tests/**" />
            </fileset>
            <metadata>
                <element name="version" value="1.0" />
            </metadata>
        </pharpackage>
    </target>

    <!-- ============================================  -->
    <!-- Target: service                             -->
    <!-- ============================================  -->
    <target name="service">
        <fail unless="repo" message="You must specify the URL to a git repository" />
        <fail unless="path" message="You must specify the path to clone the repository" />
        <property name="exists" value="0" />
        <available file="../${path}" type="dir" property="exists" value="1"/>
        <if>
            <equals arg1="${exists}" arg2="0" />
            <then>
                <exec passthru="true" command="git clone ${repo} ${path}" dir="../" checkreturn="true" />
            </then>
            <else>
                <exec passthru="true" command="git fetch origin" dir="../${path}" checkreturn="true" />
                <exec passthru="true" command="git reset --hard" dir="../${path}" checkreturn="true" />
            </else>
        </if>
    </target>

    <!-- ============================================  -->
    <!-- Target: all-services                          -->
    <!-- ============================================  -->
    <target name="all-services">
        <phingcall target="service">
            <property name="repo" value="git://github.com//* Replaced /* Replaced /* Replaced guzzle */ */ *///* Replaced /* Replaced /* Replaced guzzle */ */ */-aws.git" />
            <property name="path" value="./src//* Replaced /* Replaced /* Replaced Guzzle */ */ *//Aws" />
        </phingcall>
        <phingcall target="service">
            <property name="repo" value="git://github.com//* Replaced /* Replaced /* Replaced guzzle */ */ *///* Replaced /* Replaced /* Replaced guzzle */ */ */-cardinal-commerce.git" />
            <property name="path" value="./src//* Replaced /* Replaced /* Replaced Guzzle */ */ *//CardinalCommerce" />
        </phingcall>
        <phingcall target="service">
            <property name="repo" value="git://github.com//* Replaced /* Replaced /* Replaced guzzle */ */ *///* Replaced /* Replaced /* Replaced guzzle */ */ */-unfuddle.git" />
            <property name="path" value="./src//* Replaced /* Replaced /* Replaced Guzzle */ */ *//Unfuddle" />
        </phingcall>
    </target>

    <!-- ============================================  -->
    <!-- Target: template                              -->
    <!-- ============================================  -->
    <target name="template">
        <input propertyname="service.path" promptChar=":">Enter the path where you want to install your web service /* Replaced /* Replaced /* Replaced client */ */ */</input>
        <input propertyname="service.short_name" promptChar=":">Enter the short name of your /* Replaced /* Replaced /* Replaced client */ */ */ (e.g. /* Replaced /* Replaced /* Replaced guzzle */ */ */-aws)</input>
        <input propertyname="service./* Replaced /* Replaced /* Replaced client */ */ */_class" promptChar=":">Enter the name of your /* Replaced /* Replaced /* Replaced client */ */ */ class (e.g. UnfuddleClient)</input>
        <input propertyname="service.namespace" promptChar=":">Enter the root namespace of your /* Replaced /* Replaced /* Replaced client */ */ */ (e.g. /* Replaced /* Replaced /* Replaced Guzzle */ */ */\Aws)</input>

        <echo msg="Creating web service /* Replaced /* Replaced /* Replaced client */ */ */ project" />

        <fileset id="skeleton" dir="./template">
            <include name="**/**"/>
        </fileset>

        <echo msg="Creating folder for project at ${service.path}" />
        <mkdir dir="${service.path}" />

        <echo msg="Copying files to folder" />
        <copy todir="${service.path}" overwrite="true">
            <fileset refid="skeleton" />
            <filterchain>
                <expandproperties />
            </filterchain>
        </copy>

        <move file="${service.path}/Client.php" toFile="${service.path}/${service./* Replaced /* Replaced /* Replaced client */ */ */_class}.php" />
    </target>
</project>